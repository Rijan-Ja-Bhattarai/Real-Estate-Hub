{
  "name": "nepal-estate-index",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check script.js && node --check scripts/browser-smoke.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
